#include <cstdlib>
#include <iostream>
#include <fstream>
#include <string> 

//13:01

using namespace std;
 
int an[100000];
int ak[1000]
int main() {
    
	int n,k;
	for(;;){
		cin >> n >> k;
		if(n==0 && k==0) break;
	
		for(int i=0;i<n;i++)
			cin >> an[i];
		for(int i=0;i<k;i++)
			cin >> ak[i];
		
		
	}

    return 0;
}
